<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movium</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary" style="padding-top: 0">
        <div class="container-fluid" style="background-color: grey; height:4rem">
          <a class="navbar-brand" href="/" style="font-size: 2em;margin-left: 3rem; font-family: 'Montserrat', sans-serif;color:color-mix(in srgb, #34c9eb 20%, white)">Movium</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav" style="justify-content: flex-end;">
            <ul class="navbar-nav" style="gap: 1rem;margin-right:1rem;font-size:1.3em">
            <% if (!username) { %>
                <li class="nav-item">
                  <a class="nav-link" href="/register">Register</a>
                </li>
            <% } %>
              <li class="nav-item">
                <a class="nav-link" href="/profile/<%= profile.id %>"><%= username %></a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="<%= username ? '/logout' : '/login' %>"><%= username ? 'Logout' : 'Login' %></a>
              </li>
            </ul>
          </div>
        </div>
      </nav>

      <section style="margin: 2rem 3rem">
        <h1 style="text-align: center;">Welcome <%= profile.name %></h1><br>
        <h3>Your profile was created at: <%= dateFormat(profile.createdAt, "dddd, mmmm dS, yyyy, h:MM:ss TT") %></h3>

        <div style="display: flex; flex-direction: row; width:auto;height: fit-content;border-radius:2rem;background-color: #4A455A; padding: 2rem 2rem">
            <img style="border-radius: 2rem; width: 360px;height:360px" src="<%= profile.avatarUrl %>" alt="profile pic">
            <div style="display:flex; flex-direction: column;">
                <p style="color:white; margin-left: 2rem; font-size: 2em; width:auto"><%= profile.description %></p>

                <p style="color:white; margin-left: 2rem; font-size: 2em;">Movies watched:</p>
                <% user.Movies.forEach(el => { %>
                  <p style="color:white; margin-left: 2rem; font-size: 1.5em;">- <%= el.movieName %></p>
                <% }) %>
                <p style="color:white; margin-left: 2rem; font-size: 2em;">Upvotes: <%= numberFormat(profile.upvotes) %></p>

                <a style="margin-left: 2rem; font-size: 2em;" href="/profile/<%= profile.id %>/upvote"><button class="btn btn-primary" style="background-color: #2C157F; border:none;">Upvote</button></a>
            </div>
        </div>
      </section>

      <section style="margin: 2rem 3rem">
        <div class="container text-center">
          <div class="row" style="gap: 2rem">
            <% profile.Comments.forEach(el => { %>
            <div class="col" style="background-color: grey; border-radius: 1rem; padding:8px;">
              <h3 style="padding-bottom:6px"><%= el.title %></h3>
              <div style="display:flex; flex-direction: row; gap:1rem">
                <img style="width: 160px; height: 120px; border-radius: 6px; margin-left: 10px;" src="<%= el.imgUrl %>" alt="image">
                <p><%= el.content %></p>
              </div>
              <div style="margin-top:8px">
                <p>This comment was created at: <%= dateFormat(el.createdAt, "dddd, mmmm dS, yyyy") %></p>
              </div>
              <div style="margin-top: 8px">
                <a href="/profile/<%= profile.id %>/edit/<%= el.id %>"><button style="width:48%" class="btn btn-warning">Edit</button></a>
                <a href="/profile/<%= profile.id %>/delete/<%= el.id %>"><button style="width:48%" class="btn btn-danger">Delete</button></a>
              </div>
            </div>
            <% }) %>
            <!-- <div class="col">
              Column
            </div>
            <div class="col">
              Column
            </div> -->
          </div>
        </div>
      </section>

      <section style="margin: 2rem 3rem">
        <div style="display: flex; justify-content: center; width:auto; height:fit-content; border-radius:2rem;border: 3px solid #4A455A">
          <a href="/profile/<%= profile.id %>/comment/add"><button style="background-color:white;border:none; font-size: 1.4em">Add Comment</button></a>
        </div>
      </section>



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>